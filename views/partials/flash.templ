package partials

templ Flash(message string) {
	if message != "" {
		<div class="rounded-md border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-950" role="alert">
			{ message }
		</div>
	}
}
